@model List<TechJobsPersistent.Models.Employer>         //TODO Check if this is right

<h1>Employers Currently Hiring</h1>

@if (Model.Count == 0)
{
    <p>No employers are currently hiring!</p>
}
else
{
    <div>
        <ul>
            @foreach (Employer employer in Model)               //TODO should this be from dbcontext? or leave as is?
            {
                <li>
                    <a asp-controller="Employer" asp-action="About" asp-route-id="@employer.Id">@employer.Name</a>
                </li>
            }
        </ul>
    </div>
}